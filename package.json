{
  "name": "uniqueitemstoarray",
  "version": "1.0.0",
  "description": "Quick example of an algorithm that will find numbers unique to an array",
  "author": "Jesse Pledger",
  "type": "commonjs",
  "scripts": {
    "examples": "pnpm prepare && node build/examples/index.js",
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "pnpm run compile",
    "pretest": "pnpm run compile",
    "posttest": "pnpm run lint"
  },
  "license": "ISC",
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@jest/types": "^28.1.3",
    "@types/jest": "^28.1.5",
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.6",
    "typescript": "^4.0.3"
  }
}
